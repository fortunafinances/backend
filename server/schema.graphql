
enum OrderSide {
    Buy
    Sell
}

enum OrderType {
    Limit
    Market
    Transfer
}

enum OrderStatus {
    Placed
    Executed
}

# Trade is a buy or sell of a stock
type Trade {
    tradeID: Int!
    accID: Int!
    type: OrderType!
    side: OrderSide!
    status: OrderStatus!
    date: String!
    ticker: String!
    tradePrice: Int!
    tradeQty: Int!
}

type Holding {
    accountId: String!
    accStockId: String!
    ticker: String!
    name: String!
    stockQuantity: Int!
    price: Int!
}

type Stock {
    ticker: String!
    currPrice: Int!
    highPrice: Int!
    lowPrice: Int!
    openPrice: Int!
    prevClosePrice: Int!
}

type Query {
    trades: [Trade]!
    holdings: [Holding]!
    stocks: [Stock]!
}

type Mutation {

    insertTrade(
        tradeID: Int!
        accID: Int!
        type: OrderType!
        side: OrderSide!
        status: OrderStatus!
        date: String!
        ticker: String!
        tradePrice: Int!
        tradeQty: Int!
    ): String
}
